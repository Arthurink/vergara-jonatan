# Edición con LocalStorage y Navegación

Resumen del flujo de edición entre páginas (`index.html` → `edit.html`) usando `localStorage` con datos JSON y `editIndex`.

(Resumen importado del GitHub de la clase.)
