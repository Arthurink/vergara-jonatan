# Arquitectura Modular en JavaScript

Resumen del patrón IIFE con `App.init()`, organización en `htmlElements`, `templates`, `utils`, `handlers` y persistencia con `localStorage`.

(Resumen importado del GitHub de la clase.)
